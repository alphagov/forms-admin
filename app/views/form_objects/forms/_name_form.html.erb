<%= form_with(model: @name_form, url:) do |f| %>
  <% if @name_form&.errors.any? %>
    <%= f.govuk_error_summary %>
  <% end %>
  <%= f.govuk_text_field :name, label: {  tag: 'h1', size: 'l' } %>
  <%= f.govuk_submit t("save_and_continue") %>
<% end %>
