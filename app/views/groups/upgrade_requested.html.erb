<% set_page_title(t('page_titles.group_upgrade_requested')) %>

<% content_for :back_link, govuk_back_link_to(@group, t("back_link.group", group_name: @group.name)) %>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= govuk_panel(title_text: t("page_titles.group_upgrade_requested")) %>

    <h2 class="govuk-heading-m"><%= t("groups.upgrade_requested.what_happens_next.heading") %></h2>

    <%= t("groups.upgrade_requested.what_happens_next.body_html") %>

    <p>
      <%= govuk_link_to t("back_link.group", group_name: @group.name), group_path(@group) %>
    </p>
  </div>
</div>
