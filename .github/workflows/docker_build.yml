name: "Docker build"
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
    types: [checks_requested]
jobs:
  build_the_image:
    uses: alphagov/forms-deploy/.github/workflows/build-image.yml@use-reusable-workflow-for-build-image
