forms_api:
  # URL to form-api endpoints
  base_url: http://localhost:9292
  # Authentication key to authenticate forms-admin to forms-api
  auth_key: "123456"

# URL used to direct admin users to their form preview
runner_base: http://localhost:3001
